export enum ContextKey {
  Screen = 'screen',
  Tenant = 'tenant',
  Prompt = 'prompt',
  Organization = 'organization',
  Client = 'client',
  Transaction = 'transaction',
  User = 'user',
  UntrustedData = 'untrusted_data',
}